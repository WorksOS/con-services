version: '3'
services:
  webapi:
    build: ./Artifacts/WebApi
    ports:
     - "5000:80"
    env_file:
     - docker-compose.env
  mockprojectwebapi:
    build: ./Artifacts/MockProjectWebApi
    env_file:
     - docker-compose.env
    ports:
     - "5001:5001"
networks:
  default:
    external:
      name: nat