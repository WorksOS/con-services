apiVersion: v1
kind: Service
metadata:
  name: {{ template "projectservice.fullname" . }}
  labels:
    app: {{ template "projectservice.name" . }}
    chart: {{ template "projectservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "projectservice.name" . }}
    release: {{ .Release.Name }}
