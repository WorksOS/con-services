FROM microsoft/dotnet:2.0-sdk 

COPY . /build/
WORKDIR /build

RUN rm -rf /artifacts
RUN dotnet publish ./src/MasterDataConsumer/VSS.Productivity3D.MasterDataConsumer.csproj -o /artifacts -f netcoreapp2.0

RUN chmod 777 *.sh
RUN ["/bin/sh", "/build/unittests.sh"]