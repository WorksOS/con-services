FROM registry-docker-registry.jenkins:5000/dotnet:2.0-sdk 

COPY . /build/
WORKDIR /build

dotnet publish ./$SERVICE_PATH/TagFileHarvester.netcore/TagFileHarvester.netcore.csproj -o /artifacts --framework netcoreapp2.0 --runtime linux-x64

