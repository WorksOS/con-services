namespace VSS.TRex.Common.Interfaces
{
  /// <summary>
  /// Provides an annotation/interface to indicate that even though the implementer may
  /// inherit the IBinarizable interface, it intentional declines to support it.
  /// </summary>
  public interface INonBinarizable
  {
  }
}
