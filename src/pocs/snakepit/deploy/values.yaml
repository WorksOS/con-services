# Default values for projectservice.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
environment: dev

rootDomain: k8s.vspengg.com

replicaCount: 1

image:
  repository: "276986344560.dkr.ecr.us-west-2.amazonaws.com/vss-landfill-snakepit"
  tag: latest

resources:
  requests:
   cpu: 250m
   memory: 2Gi

tolerations: []

ingress:
  enabled: true
  # Used to create an Ingress record. environment name will be appended
  hosts:
    - snakepit
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-connect-timeout: 3600
    nginx.ingress.kubernetes.io/proxy-send-timeout: 3600
    nginx.ingress.kubernetes.io/proxy-read-timeout: 3600
    external-dns.alpha.kubernetes.io/target: endpoints.k8s.vspengg.com
