using System;
using System.Collections.Generic;
using System.Text;

namespace VSS.Map3D.Common
{
  class QMHeader
  {
    /*
    public static  void GetDegrees(double x, double y, double z, out double latitude, out double longitude)
    {
      double deg = 0.01745329252;
      double r =  Math.Sqrt(x ^ 2 + y ^ 2 + z ^ 2)
      longitude = Math.Asin(y / r) / deg;
      if (longitude > 180d)
        longitude = 180d - longitude;
      latitude = Math.Atan(z / r) / deg;
    }
    */
  }


}
